name: Auto Merge Develop → testing

on:
  workflow_run:
    workflows: ["CI - Backend & Frontend Tests"]
    types:
      - completed

permissions:
  contents: write
  actions: write

jobs:
  merge:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'develop'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: testing
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

      - name: Merge develop into testing
        run: |
          git fetch origin
          git checkout testing
          git pull origin testing
          git merge origin/develop --no-edit
          git push origin testing

      # - name: Trigger CI on testing   # ✅ THIS FIXES YOUR ISSUE
      #   run: |
      #     gh workflow run "CI - Backend & Frontend Tests" --ref testing
      #     gh workflow run "Testing Branch Workflow"
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
