/**
 * @param Bhoj Gc
 * The following interface is just a sample to send the shoppers registration to backend for storage
 */
export interface RegisterUser {
  userName: String;
  userEmailAddress: String;
}
