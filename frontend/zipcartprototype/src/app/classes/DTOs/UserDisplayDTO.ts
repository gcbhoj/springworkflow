/**
 * @param Bhoj Gc
 * The following interface will be used to display the details of the logged in user
 * It can also be used to pass user data between components
 */
export interface UserDisplay {
  userId: string;
  userName: string;
}
